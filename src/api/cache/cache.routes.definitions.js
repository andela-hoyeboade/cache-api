export default {
    "'DELETE' /api/cache/'": "Removes all keys from the cache",
    "'GET' /api/cache/": "Return all stored keys",
    "'GET' /api/cache/<key>": "Returns the cached data for a given key",
    "'POST' /api/cache/<key>/'": "Creates the data for a given key",
    "'PUT' /api/cache/<key>/'":  "Updates the data for a given key",
    "'DELETE' /api/cache/<key>/'": "Removes a given key from the cache",
}